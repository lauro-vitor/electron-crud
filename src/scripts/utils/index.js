const validaOnKeyDown = require('./validateOnkeyDonw');
const writeMessage = require('./writeMessage');
const clearMessage = require('./clearMessage');

module.exports = {
    validaOnKeyDown,
    writeMessage,
    clearMessage,
}